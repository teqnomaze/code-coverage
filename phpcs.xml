<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" namespace="Teqnomaze" name="Coverage Check">

	<!-- Pass some flags to PHPCS:
		 p flag: Show progress of the run.
		 s flag: Show sniff codes in all reports.
	-->
	<arg value="ps"/>

    <!-- Format output styling -->
    <arg name="colors"/>

	<!-- Strip the filepaths down to the relevant bit. -->
	<arg name="basepath" value="./"/>

	<!-- Check up to 8 files simultanously. -->
	<arg name="parallel" value="8"/>

	<!-- Only check the PHP files. JS, and CSS files are checked separately with @wordpress/scripts package. -->
	<arg name="extensions" value="php"/>

	<!-- Check all files in this directory and the directories below it. -->
    <file>bin</file>
	<file>src</file>
	<file>tests</file>

    <!-- Base standard -->
    <rule ref="PSR1"/>
    <rule ref="PSR2"/>

    <rule ref="PSR12">
        <!-- Exclude the specific sniff -->
        <exclude name="PSR12.Files.FileHeader.SpacingAfterBlock" />
    </rule>

    <rule ref="Squiz.Commenting">
        <!-- Exclude the specific sniff -->
        <exclude name="Squiz.Commenting.FileComment.IncorrectCopyright"/>
        <exclude name="Squiz.Commenting.FileComment.MissingAuthorTag"/>
        <exclude name="Squiz.Commenting.FileComment.IncorrectAuthor"/>
        <exclude name="Squiz.Commenting.FileComment.MissingSubpackageTag"/>
        <exclude name="Squiz.Commenting.FileComment.SubpackageTagOrder"/>
        <exclude name="Squiz.Commenting.FileComment.MissingCopyrightTag"/>
        <exclude name="Squiz.Commenting.ClosingDeclarationComment.Missing"/>
    </rule>

    <!-- Additional rulesets -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="150"/>
        </properties>
    </rule>

</ruleset>
